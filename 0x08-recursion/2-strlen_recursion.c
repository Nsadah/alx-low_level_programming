/**
* _strlen_recursion - prints to string
* Description: Prints to string
*@s: the value it receives
* Return: Always (Success)
*/
#include <stdio.h>
#include <string.h>
int _strlen_recursion(char *s)
{
int len;
len = strlen(s);
return (len);
}
