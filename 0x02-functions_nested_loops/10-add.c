/**
 * add - prints to int
 * Description: Prints alphabet in lowercase
 *@a: the integer value it receives
 *@b: the integer it rescieves
 * int add(int a, int b)
 *return: r
 */
#include "main.h"
#include <stdio.h>
int add(int a, int b)
{
int r = a + b;
return (r);
}
