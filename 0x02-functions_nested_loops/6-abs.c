/**
 * _abs - prints to int
 * Description: prints absolute value
 *@n: the integer value it receives
 * Return: 0
 */
#include "main.h"
#include <stdlib.h>
#include <stdio.h>
int _abs(int N)
{
if(N < 0 || N > 0)
{
printf("1\n");
}
else
{
printf("0");
}
return (0);
}
