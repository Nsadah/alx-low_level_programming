/**
 * _abs - prints to int
 * Description: prints absolute value
 *@n: the integer value it receives
 * Return: 0
 */
#include "main.h"
#include <stdlib.h>
#include <stdio.h>
int _abs(int n)
{
if (n < 0)
{
return (-n);
}
else if (n > 0)
{
return (n);
}
else
{
return (n);
}
}
