/**
* print_last_digit - prints to int
* Description: prints last digit
*@n: the integer value it receives
* Return: 0
*/
#include "main.h"
#include <stdio.h>
int _isupper(int c)
{
for (c = 'A'; c <= 'Z'; c++)
{
if (c => 'A' || c <= 'Z')
{
_putchar (C);
return (1);
}
else
{
return (0);
}
}
