#include <stdio.h>
/**
 * main - prints to string
 * Description: Prints alphabet in lowercase
 * Return: 0
 */
int main(void)
{
int ch;
for (ch = 'a'; ch <= 'z'; ch++)
{
putchar(ch);
}
return (0);
}
